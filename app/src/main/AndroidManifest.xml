<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zdww.jssl_test">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jssltest">
        <activity android:name=".musicsList.MusicsActivity"></activity>
        <!-- 使用jetpack样例 -->
        <activity android:name=".musicsList_jetpack.MusicsJetActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".player.FlowPlayerControllerActivity" />
        <activity android:name=".player.PlayerActivity">
        </activity>
        <activity android:name=".jetpack.simple.livedata.TestLiveDataActivity">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>